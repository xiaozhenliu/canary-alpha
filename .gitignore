# Dependencies
node_modules/

# Build outputs
.dist/
dist/
build/
coverage/
*.tsbuildinfo

# VitePress
docs/.vitepress/dist/
docs/.vitepress/cache/

# Environment files
.env
.env.*
!.env.example
!.env.*.example

# Local IDE state
.zed/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Caches and temp files
.cache/
.tmp/
tmp/
# Repo-internal test temp root (see tests/helpers/test-tmp.ts)
.test-tmp/

# Local databases
*.db
*.sqlite
*.sqlite3

# Local runtime state
.worktrees/
.canary-alpha-mcp/
.computer-history-mcp/
.omo/
.commandcode/
.screenpipe-memory-mcp/ # legacy local data dir (pre-rename)
.codex-review/
.claude/
.mcp.json
# Retired GSD planning state — kept on disk locally, no longer tracked.
.planning/

# Local Screenpipe development artifacts and private operator notes
/screenpipe-*.tar.gz
/screenpipe-*.tar.gz.sha256
/mlx.metallib
/docs/engineering/screenpipe-version-development.md

# Experiments — track source/fixtures; ignore secrets, runtime artifacts, and
# probe dumps (probe_parse_result.json embeds bce-auth signed URLs + access-key id)
experiments/ocr-compare/results/
experiments/ocr-compare/logs/
experiments/ocr-compare/.venv*/
experiments/ocr-compare/models/
experiments/ocr-compare/cache/
experiments/ocr-compare/probe_parse_result.json

# Python bytecode
__pycache__/
*.pyc

# OS noise
.DS_Store

# understand-anything: track only the generation inputs (config + ignore rules);
# ignore all regenerable artifacts (knowledge graph, metadata, intermediate batches,
# trash, fingerprints, dashboard runtime) — regenerate locally via `understand`.
.understand-anything/*
!.understand-anything/config.json
!.understand-anything/.understandignore
.codex/*
!.codex/agents/
!.codex/agents/**
!.codex/skills/
!.codex/skills/**
.codex/skills/.system/**
!.codex/prompts/
!.codex/prompts/**

# Local operator release & history rewrite plans
docs/engineering/public-history-rewrite-plan.md
.scratch/
