{
  "_comment": "Ground truth for ide-vscode fixture. App: VS Code (Code), window: TypeScript file editor. Universal structured extraction preserves the window title and labels the AXWebArea body.",
  "frame_id": 1001,
  "extractedText": "[Window] work-activity-analysis.ts — computer-history-mcp\n[Body] import { ExtractionRegistry } from './extraction/registry';\n[Body] import { SessionAggregator } from './sessions/aggregator';\n[Body] export class WorkActivityAnalysisService {\n[Body] constructor(\n[Body] private readonly extractionRegistry: ExtractionRegistry,\n[Body] private readonly sessionAggregator: SessionAggregator\n[Body] ) {}\n[Body] async processFrame(frame: ScreenPipeFrame): Promise<void> {\n[Body] const extraction = this.extractionRegistry.extract(toExtractionInput(frame));\n[Body] await this.sessionAggregator.handleExtraction(extraction);\n[Body] }\n[Body] }",
  "extractionRuleKind": "generic",
  "contextLabel": "work-activity-analysis.ts — computer-history-mcp",
  "contextKey": "code::work-activity-analysis.ts — computer-history-mcp",
  "session_count": 1,
  "session_boundaries": [
    {
      "app_name": "Code",
      "context_key": "code::work-activity-analysis.ts — computer-history-mcp",
      "frame_ids": [1001]
    }
  ],
  "find_hit_frame_ids": [1001],
  "find_queries": [
    "ExtractionRegistry",
    "WorkActivityAnalysisService",
    "processFrame"
  ]
}
